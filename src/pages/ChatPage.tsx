import React from 'react';
import Chat from '@/components/Chat';

const ChatPage: React.FC = () => {
    return (
        <div className="h-full w-full animate-fadeSlide">
            <Chat />
        </div>
    );
};

export default ChatPage;
