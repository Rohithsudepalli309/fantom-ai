# Copy this file to .env.local (preferred) or .env and set your keys
# .env.local is git-ignored in this repo (.gitignore has *.local)

# Required for text/chat/vision (Google AI Studio / Gemini)
VITE_GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# Optional: free image-generation fallback via Hugging Face Inference API.
# If set, the app will use Hugging Face first for images; otherwise it will
# attempt Google Imagen (may require billing). You can also switch to the
# "Nano" model in the UI to avoid Imagen entirely.
#VITE_HF_API_TOKEN=your_hugging_face_token_here
#VITE_HF_IMAGE_MODEL=stabilityai/sdxl-turbo

# Auth provider selection
# VITE_AUTH_PROVIDER can be: local | supabase | firebase | auto (auto-pick based on configured envs)
#VITE_AUTH_PROVIDER=auto

# Security controls
# Require a fresh login after signup (recommended)
#VITE_REQUIRE_LOGIN_AFTER_SIGNUP=true
# Disable public signup in production (set to false to block)
#VITE_AUTH_ALLOW_SIGNUP=true
# Optional idle timeout (minutes) for auto sign-out on inactivity
#VITE_AUTH_IDLE_TIMEOUT_MIN=30
