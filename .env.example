# Copy this file to .env.local (preferred) or .env and set your values
# .env.local is git-ignored in this repo (.gitignore has *.local)

# Text/Chat/Vision: if you still use Gemini features, set this.
#VITE_GEMINI_API_KEY=

# Image Generation: Local Stable Diffusion (Automatic1111)
# - Start A1111 with: --api --cors-allow-origins=http://localhost:5173,http://127.0.0.1:5173
# - Default endpoint below, override if different
VITE_LOCAL_SD=http://127.0.0.1:7860
VITE_USE_LOCAL_AI=true

# Optional: Stability API (not used by default)
#VITE_STABILITY_API_KEY=
# Dev server transport
# Use HTTPS with mkcert by default. Set false to use HTTP (no cert).
#VITE_DEV_HTTPS=true


# Supported image models (select in UI, ensure installed in A1111):
# - stabilityai/stable-diffusion-xl-base-1.0
# - stable-diffusion-v1-5/stable-diffusion-v1-5
# - Lykon/DreamShaper
# - CompVis/stable-diffusion-v1-4

# Auth provider selection
# VITE_AUTH_PROVIDER can be: local | supabase | firebase | jwt | auto (auto-pick based on configured envs)
#VITE_AUTH_PROVIDER=auto
# Example to enable JWT auth backend (local server in /server):
#VITE_AUTH_PROVIDER=jwt
# For the JWT server, set a secret (used by /server/index.cjs)
#JWT_SECRET=change-me-in-local-dev

# AI provider selection (text/chat/vision)
# VITE_AI_PROVIDER can be: gemini | nemotron | local
# - gemini: uses Gemini API key features
# - nemotron: NVIDIA Nemotron Nano 12B 2 VL (text + vision only)
# - local: Ollama local LLM (requires local server at http://localhost:11434)
#VITE_AI_PROVIDER=gemini

# NVIDIA Nemotron (text + vision)
#VITE_NVIDIA_API_KEY=
#VITE_NVIDIA_BASE_URL=https://api.nvidia.com
#VITE_NVIDIA_MODEL=nvidia/nemotron-nano-12b-v2-vl
# Optional Nemotron tuning (can also set at runtime via window/localStorage)
#VITE_NEMOTRON_MAX_TOKENS=1024
#VITE_NEMOTRON_VISION_MAX_TOKENS=800
# To enable Nemotron as the active provider:
#VITE_AI_PROVIDER=nemotron

# Optional NVIDIA endpoints for image/video (if available). When set, image/video will use these.
#VITE_NVIDIA_IMAGE_URL=https://api.nvidia.com/v1/images/generations
#VITE_NVIDIA_VIDEO_URL=https://api.nvidia.com/v1/video/generations

# Security controls
# Require a fresh login after signup (recommended)
#VITE_REQUIRE_LOGIN_AFTER_SIGNUP=true
# Disable public signup in production (set to false to block)
#VITE_AUTH_ALLOW_SIGNUP=true
# Optional idle timeout (minutes) for auto sign-out on inactivity
#VITE_AUTH_IDLE_TIMEOUT_MIN=30
